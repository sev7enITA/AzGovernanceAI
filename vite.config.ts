/**
 * Vite Configuration — M365 Copilot Governance Dashboard
 *
 * Uses relative base path ('./') so the built app works when deployed
 * to any directory on any host (Hostinger, Netlify, GitHub Pages, etc.)
 *
 * @license MIT
 */
import { defineConfig } from 'vite';

export default defineConfig({
    base: './',
});
